// CALCULATE BRIGHTNESS IPO CALCULATOR BY JONATHAN

// Input
let R = +prompt("Enter an R color value (Red):");
let G = +prompt("Enter a G color value (Green):");
let B = +prompt("Enter a B color value (Blue):");

console.log(R);
console.log(G);
console.log(B);

// Process

// Output
